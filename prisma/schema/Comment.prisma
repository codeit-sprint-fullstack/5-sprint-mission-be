model Comment {
  id         String    @id @default(uuid())
  content    String
  authorId   Int
  nickname   String
  createdAt  DateTime  @default(now())
  updatedAt  DateTime? @updatedAt
  targetId   String
  targetType String
}
