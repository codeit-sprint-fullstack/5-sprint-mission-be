model Products {
  id          String   @id @default(uuid())
  userId      String
  images      String[]
  tags        String[]
  price       Int
  name        String
  description String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
