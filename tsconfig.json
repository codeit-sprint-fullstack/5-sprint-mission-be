{
  "compilerOptions": {
    "target": "ESNext",                      // 최신 ECMAScript 기능 사용
    "module": "CommonJS",                     // Node.js에서 CommonJS 모듈 사용
    "moduleResolution": "node",               // Node.js 방식으로 모듈 해결
    "esModuleInterop": true,                  // ES 모듈과 CommonJS 모듈 호환
    "skipLibCheck": true,                     // 라이브러리 파일 체크 생략
    "strict": true,                           // 엄격한 타입 검사
    "forceConsistentCasingInFileNames": true, // 대소문자 구분 파일 경로 처리
    "outDir": "./dist",                       // 컴파일된 JavaScript 파일 출력 디렉토리
    "baseUrl": ".",                           // 상대 경로의 기준이 되는 디렉토리 설정
    "paths": {
      "*": ["node_modules/*", "src/types/*"]
    }
  },
  "include": ["src/**/*.ts"],                 // 컴파일할 TypeScript 파일 경로
  "exclude": ["node_modules"]                 // 제외할 디렉토리
}